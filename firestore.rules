rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return signedIn()
        && request.auth.token.email != null
        && request.auth.token.email in [
          'javitid@gmail.com'
        ];
    }

    match /easy/{document=**} {
      allow read: if signedIn();
      allow write: if isAdmin();
    }

    match /medium/{document=**} {
      allow read: if signedIn();
      allow write: if isAdmin();
    }

    match /hard/{document=**} {
      allow read: if signedIn();
      allow write: if isAdmin();
    }

    match /prueba/{document=**} {
      allow read: if signedIn();
      allow write: if isAdmin();
    }

    match /games/{gameId}/levels/{level}/cards/{document=**} {
      allow read: if signedIn();
      allow write: if isAdmin();
    }

    match /config/{document=**} {
      allow read, write: if isAdmin();
    }

    match /leaderboards/{language}/levels/{level}/times/{document=**} {
      allow read, write: if signedIn();
    }

    match /leaderboardsByGame/{gameId}/languages/{language}/levels/{level}/times/{document=**} {
      allow read, write: if signedIn();
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
